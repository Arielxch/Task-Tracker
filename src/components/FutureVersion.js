import { Link } from 'react-router-dom';

const FutureVersion = () => {
  return (
    <div>
      <h4>Version x.x.x</h4>
      <Link to='/'>Go Back</Link>
    </div>
  );
};

export default FutureVersion;